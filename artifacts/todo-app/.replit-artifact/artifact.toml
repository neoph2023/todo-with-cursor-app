kind = "web"
previewPath = "/"
title = "Lista de tareas"
description = "Una aplicación sencilla y elegante en español para añadir, completar, editar y eliminar tareas cotidianas."
version = "1.0.0"
id = "artifacts/todo-app"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25230

[services.development]
run = "pnpm --filter @workspace/todo-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/todo-app", "run", "build" ]
serve = "static"
publicDir = "artifacts/todo-app/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25230"
BASE_PATH = "/"
